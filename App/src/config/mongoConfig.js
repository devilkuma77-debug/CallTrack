/** Phone → Atlas Data API ya cloud HTTPS — hotspot ki zarurat nahi */
export const MONGO_DIRECT_ATLAS = true;
export const MONGO_ENABLED = true;
export const MONGO_API_URL = '';

console.log('Mongo sync: Phone → cloud → MongoDB (no hotspot)');
